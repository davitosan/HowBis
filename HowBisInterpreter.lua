--ClassID, Required Rune Engraving, SlotName, ItemID, BiS
function HBIS:AddItem(classID, spellIDs, slotNames, ItemID, BiS)
	--print("ClassID: " .. classID .. ". Runes: " .. HBIS:dump(spellIDs) .. ". SlotNames: " .. HBIS:dump(slotNames) .. ". ItemID: " .. ItemID .. ". Bis: " .. tostring(BiS))

	--HBIS.BiSListsByClass[classID] = 
end